import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'net.helperlesson.mobile',
  appName: 'Helper',
  webDir: 'dist',
  backgroundColor: '#f4f7f6',
}

export default config
